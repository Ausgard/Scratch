import Swiper from 'swiper';
let swiper = new Swiper('.swiper-container', {
    slidesPerView: 'auto',
    loop: true,
    effect: 'slide',
    spaceBetween: 20,
    centeredSlides: true,
    centeredSlidesBounds: true,
  });
